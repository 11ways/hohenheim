{#- Databases tab on a site: attached managed databases whose connection
    details are injected into the site's processes as env variables. -#}
{% declare title: String %}
{% declare siteId: Integer %}
{% declare siteName: String %}
{% declare links: List %}
{% declare recordTabs: List<RecordTabState> %}
{% declare flashMessage: Microcopy = null %}
{% extend "zenitcms:shell" var:title=title var:flashMessage=flashMessage %}

{% block "main" %}
<section class="cms-resource-list-page">
    <header class="cms-resource-list-header">
        <h1>{{ siteName }}</h1>
        <a href={% "/admin/site-databases/new?site_id=" + siteId %} id="attach-database-link">
            <pl-button variant={% "primary" %}>{{ t("attach", scope: "site_databases") }}</pl-button>
        </a>
    </header>

    {% render "zenitcms:record-tabs" var:tabs=recordTabs %}

    {% if links %}
    <pl-table>
        <pl-table-header><pl-table-row>
            <pl-table-head>{{ t("database", scope: "site_databases") }}</pl-table-head>
            <pl-table-head>{{ t("engine", scope: "site_databases") }}</pl-table-head>
            <pl-table-head>{{ t("status", scope: "site_databases") }}</pl-table-head>
            <pl-table-head>{{ t("env_prefix", scope: "site_databases") }}</pl-table-head>
            <pl-table-head>{{ t("variables", scope: "site_databases") }}</pl-table-head>
        </pl-table-row></pl-table-header>
        <pl-table-body>
            {% each links as index, link %}
            <pl-table-row>
                <pl-table-cell>
                    <a href={% link.get("editUrl") %}>{{ link.get("dbName") }}</a>
                    {% if link.get("primary") == true %}
                    <pl-badge variant={% "outline" %}>{{ t("primary", scope: "site_databases") }}</pl-badge>
                    {% /if %}
                </pl-table-cell>
                <pl-table-cell>{{ link.get("engine") }}</pl-table-cell>
                <pl-table-cell data-db-status={% link.get("status") %}>
                    {% if link.get("status") == "active" %}
                    <a href={% link.get("dbUrl") %}><pl-badge variant={% "success" %}>{{ t("active", scope: "site_databases") }}</pl-badge></a>
                    {% elseif link.get("status") == "failed" %}
                    <a href={% link.get("dbUrl") %}><pl-badge variant={% "destructive" %}>{{ t("failed", scope: "site_databases") }}</pl-badge></a>
                    {% elseif link.get("status") == "provisioning" %}
                    <a href={% link.get("dbUrl") %}><pl-badge variant={% "warning" %}>{{ t("provisioning", scope: "site_databases") }}</pl-badge></a>
                    {% else %}
                    <pl-badge variant={% "secondary" %}>{{ link.get("status") }}</pl-badge>
                    {% /if %}
                </pl-table-cell>
                <pl-table-cell><code>{{ link.get("prefix") }}</code></pl-table-cell>
                <pl-table-cell><code>{{ link.get("varNames") }}</code></pl-table-cell>
            </pl-table-row>
            {% /each %}
        </pl-table-body>
    </pl-table>
    {% else %}
    <pl-empty-state icon="database">
        <pl-empty-state-title>{{ t("empty", scope: "site_databases") }}</pl-empty-state-title>
        <pl-empty-state-description>{{ t("empty_description", scope: "site_databases") }}</pl-empty-state-description>
    </pl-empty-state>
    {% /if %}
</section>
{% /block %}
