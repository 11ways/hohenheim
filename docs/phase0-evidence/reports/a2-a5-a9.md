Commit `d62ef3b` is clean and the worktree has no leftover changes. Final report:

```text
Issue: A2 - `zenit-dev build --clean` deletes output before taking the directory lock
User-facing behavior changed: `build --clean`/`--force`/stale-jar cleans now block on the
  per-realpath directory Gradle exclusion before deleting build/generated-sources and TeaVM
  output, and hold it across delete + rebuild. A concurrent test/build gradle run in the same
  directory can no longer have its outputs deleted underneath it.
Architectural responsibility changed: output MUTATION now rides the SAME lock mechanism as
  Gradle execution (acquireGradleDirLock, re-entrant into the spawn funnels), not an ordering
  convention. cleanBuildArtifacts hard-errors ("BUG: ... without holding the per-directory
  Gradle exclusion") if any future caller skips it. Lock order unchanged
  (repo build lock > dir lock > slot); dir-lock AIDEV-NOTE extended to state the doctrine.
Files changed: /home/skerit/projects/javaweb/zenit/tools/zenit-dev (cleanBuildArtifacts guard,
  buildSelf dir-lock hold, AIDEV-NOTEs)
Tests added or changed: /home/skerit/projects/javaweb/zenit/tools/zenit-dev.test.js -
  "build --clean waits for the directory Gradle exclusion before deleting outputs": isolated
  fixture workspace (.zenit-dev.json, fake ./gradlew), live `test --unit --skip-deps` whose
  fake gradle task creates a generated-sources marker and sleeps 6s inside the dir lock,
  concurrent `build --clean --skip-deps`; asserts the marker survives, both exits 0, the clean
  still runs afterward and deletes the outputs.
Observed pre-fix failure: AssertionError "the clean deleted generated sources under a live
  gradle run in the same directory" - actual 'DELETED', expected 'SURVIVED' (verdict written by
  the in-flight gradle task when its marker vanished mid-run).
Verification command and result: `node --test --test-name-pattern "directory Gradle exclusion
  before deleting" zenit-dev.test.js` - pass (8.2s); full file 25/25 pass.
Backend/browser coverage: n/a (node CLI).
Known limitations or decisions: the exclusion protects zenit-dev-spawned mutations; a human
  running rm by hand is still outside it (already forbidden by workspace rules).
Commit and push state: zenit d62ef3b, NOT pushed.
```

```text
Issue: A5 - Optional dependency builds are flat, non-recursive, and non-topological
User-facing behavior changed: dependency discovery now builds a transitive DAG. Editing a lib
  two hops away (app -> zenit-cms -> zenit-forms -> plumage) marks and rebuilds the whole
  affected chain in topological order, each stale repo exactly once; previously the change was
  invisible (dep lists covered only libs named in the consumer's own build file, appended in
  hardcoded OPTIONAL_LIBRARIES order).
Architectural responsibility changed: discovery is DECLARATION-based, not whole-file substring
  scan: comment-stripped lines matching `be.elevenways:<lib>[-common|-client|-server]:` or the
  catalog alias `libs.<lib.dots>[...]`. test/browserTest configurations are WEAK edges (feed
  the root project's own dep list + fingerprint, never propagate transitively, never constrain
  order - the zenit-cms test-scoped zenit-pages precedent). A cycle over STRONG edges dies
  loudly naming the cycle. depsForProject returns the topologically sorted transitive closure
  and remains THE single source for build order, fingerprints, status and dep checks, so
  freshness fingerprints now cover every dependency's effective transitive inputs. Phantom
  comment edges are gone (zenit-media "depended on" zenit-cms via a comment; zenit-auth on
  zenit-pages likewise). Real-workspace closures verified acyclic and sane for 12 repos.
Files changed: zenit/tools/zenit-dev (libRefPattern/stripGradleComments/scanOptionalDeps/
  directDepsFor/depsForProject; exports).
Tests added or changed: zenit-dev.test.js - "optional dependency chains rebuild transitively in
  topological order" (fixture A->B->C: first build publishes cores then plumage, zenit-forms,
  zenit-cms, app; touching plumage republishes exactly [plumage, zenit-forms, zenit-cms, app]),
  "dependency scanning is declaration-based and scope-aware" (comments ignored, weak vs strong,
  zenit-auth-test-support does not shadow-match zenit-auth), "a strong dependency cycle fails
  loudly instead of picking an order" (subprocess `status`, exit 1, /Dependency cycle/).
Observed pre-fix failure: deepStrictEqual - actual ['protoblast','emberglyph','hawkeye',
  'janeway','zenit','zenit-cms','fixapp'], expected includes 'plumage','zenit-forms' (transitive
  deps never discovered/built); second-run delta was empty (C's change invisible).
Verification command and result: `node --test zenit-dev.test.js` from the zenit repo (the exact
  zenitDevTest invocation) - 25/25 pass. Live `zenit-dev status` in zenit-cms runs the new
  closure against real build files without error.
Backend/browser coverage: n/a.
Known limitations or decisions: (1) one flake found and fixed during CI verification: real
  same-named gradle daemons on the machine make killRepoDaemons issue its sanctioned
  `./gradlew --status` query, which the fixture log initially counted as a build - the test now
  filters `--status` lines. (2) Fingerprint scheme change + this commit make downstream repos
  report stale once; the CI reruns already republished the chain, and the final commit triggers
  one more one-time republish wave on the next build (content-based freshness self-heals).
  (3) Scanning stays string/regex-based (stdlib-only CLI); a dependency declared via exotic
  Gradle DSL indirection would be missed exactly as before.
Commit and push state: zenit d62ef3b, NOT pushed.
```

```text
Issue: A9 - Workspace CI omits converted deployables
User-facing behavior changed: `zenit-dev ci` now builds and tests herald, spamservice, thoth,
  and quirkyquarters (all converted -common/-client/-server consumers in the delivered change
  set) at the final level, after their zenit-comms/zenit-auth/zenit-ai/zenit-a2ui dependencies.
Architectural responsibility changed: CI membership is now PINNED: new exported CI_EXCLUDED map
  documents deliberate exclusions with reasons (arcana: pre-catalog build shape; hohenheim:
  outside FRAMEWORK_ROOT, built/tested from its own checkout), and a test enforces that every
  workspace repo with a build file (top-level plus nested orcono/mvp-v01 discovery) is either
  in CI_LEVELS or in CI_EXCLUDED - a new repo can no longer be silently forgotten. A second
  test asserts CI_LEVELS is topological over strong dependency edges using the A5 scanner.
Files changed: zenit/tools/zenit-dev (CI_LEVELS last level, CI_EXCLUDED, exports CI_LEVELS/
  CI_EXCLUDED/depsForProject/directDepsFor).
Tests added or changed: zenit-dev.test.js - "CI levels pin every workspace repo as a deliberate
  inclusion or exclusion" (also asserts herald/spamservice/thoth present and hohenheim pinned
  as an exclusion), "CI levels are topological over strong dependency edges".
Observed pre-fix failure: TypeError "Cannot read properties of undefined (reading 'flat')"
  (CI_LEVELS/CI_EXCLUDED unexported/nonexistent); with exports alone the pin would fail on
  herald/spamservice/thoth/quirkyquarters being neither included nor excluded.
Verification command and result: pin tests pass; live runs: `zenit-dev ci
  --projects=herald,spamservice,thoth` -> CI PASSED (spamservice 127, herald 2, thoth 10 unit
  tests; run 20260731-112035; the first attempt failed only on the A5 fixture flake noted
  above, then republished the stale chain and passed), `zenit-dev ci --projects=quirkyquarters`
  -> CI PASSED (1462 unit tests; run 20260731-112429). Named risk covered: a listed entry that
  cannot build/test would make ci permanently red.
Backend/browser coverage: ci unit mode only (its default); thoth/quirkyquarters browser suites
  run under `ci --browser`, not exercised here.
Known limitations or decisions: quirkyquarters added beyond the ledger's named three because it
  is a converted deployable in the workspace (judgment call, proven green). Hohenheim was NOT
  added: ci resolves projects under FRAMEWORK_ROOT; supporting external roots is a structural
  extension the ledger only conditions on ("if it's part of CI") - pinned as a deliberate,
  documented exclusion instead.
Commit and push state: zenit d62ef3b, NOT pushed.
```

One process note: the initial commit merged subject and body into one paragraph (missing blank line); I amended the seconds-old, unpushed commit once to restore the subject/body split — final hash d62ef3b, message is subject + blank + one body line, gitmoji first character.