Committed (not pushed). Final report:

```text
Issue: C14 - Route claim uniqueness does not cover overlapping listener sets

User-facing behavior changed:
Two simultaneous writers claiming one hostname+path with OVERLAPPING but
unequal listener restrictions (an all-interfaces row vs a single-address row)
could both commit; the dispatcher then silently dropped one tenant's route.
Now the second writer queues behind the first's write transaction and is
refused with the existing readable violation (route_taken_other_site /
enable_route_conflict microcopy keys, unchanged). Restoring a soft-deleted
site into a taken route is refused writing nothing; a site write that fails
after the claim restamp rolls its claim stamps back.

Architectural responsibility changed:
Route ownership is now a transactionally SERIALIZED claim registry (the
ledger's second named direction). SiteModel.save and SiteDomainModel.save
DECLARE a write transaction, so the conflict scan (beforeValidate), the claim
stamp (beforeWrite) and the row write are one unit; hohenheim's engine (SQLite
only, enforced by HohenheimDatabase.requireSqlite; BEGIN IMMEDIATE single
writer since zenit 6125ef1) serializes write transactions, so the scan is
authoritative, never advisory. Previously SiteDomainModel.save ran with NO
transaction at all and SiteModel's transactionality was an undeclared
RevisionableBehaviour side effect. The unique live_route_key index stays as
the storage-level equality backstop.

Model chosen and why the alternative lost:
The prompt's "expand any to configured listeners" candidate is structurally
unsound: listen_on is a bare StringField validated against the discovered
address set only on the CMS form path (dynamic Select), so the direct writers
the invariant exists for (seeder, import, API, revision restore) can store any
address, and an empty restriction conflicts with EVERY address including ones
outside the discovered set -- no finite expansion covers that. The discovered
set is also refreshed hourly (UpdateSystemIpAddresses), so expansions go stale
on every interface change and need background restamps with a silent-false
window. Wildcard-vs-any-value overlap is not expressible as row equality
without a closed, stable universe; there is none here. Only one structurally
correct model remained, so no operator escalation.

Migration version used: NONE. No schema change: the M045 column + unique
index are retained unchanged, so the checksum golden is untouched (verified by
MigrationIntegrityTest post-fix).

Files changed (all /home/skerit/projects/hohenext/hohenheim):
- src/common/java/be/elevenways/hohenheim/model/SiteDomainModel.java
  (transactional save override; LIVE_ROUTE_KEY AIDEV-NOTE rewritten)
- src/common/java/be/elevenways/hohenheim/model/SiteModel.java
  (declared transactional save, no longer riding the revisionable coincidence)
- src/server/java/be/elevenways/hohenheim/server/proxy/RouteClaims.java
  (the "residual gap, deliberately accepted" AIDEV-NOTE at :33-38 rewritten to
  the new serialized-registry truth, incl. the 5s busy_timeout constraint)
- src/server/java/be/elevenways/hohenheim/server/cms/SiteDomainResource.java,
  SiteResource.java (hook docblocks: "advisory" wording corrected)
- src/browserTest/.../RouteOwnershipInvariantTest.java (+2 tests, helpers,
  nested SiteWriteFault injector)
- src/browserTest/.../SiteDomainWriteBarrier.java (new; reuses
  SiteEnableWriteBarrier.Coordinator; one-hook-per-JVM contract preserved)
The 9dd6de4 test adaptation was kept intact.

Tests added or changed:
- anAllInterfacesRowAndASingleAddressRowCannotBothClaimOneHostname: barriered
  domain-save race, parks writer A after its scan, races writer B with an
  overlapping single-address claim.
- wildcardShadowingRestoreAndFailedWritesKeepRouteStorageExact: wildcard/exact
  shadowing refusal, restore-into-taken refused writing nothing,
  restore-after-free reclaims, fault-injected enable rolls claim stamps back
  and stays retryable.

Observed pre-fix failure (exact):
  RouteOwnershipInvariantTest.anAllInterfacesRowAndASingleAddressRowCannotBothClaimOneHostname()
  [step 5: exactly one stored claim on the contested route -- an all-interfaces
  and a single-address claim must never coexist]
  org.opentest4j.AssertionFailedError: expected: 1L / but was: 2L

Verification command and result:
- zenit-dev test --browser --class RouteOwnershipInvariantTest -> 5/5 passed
- zenit-dev test --browser --class PathRoutingTest --class DomainEditTest
  --class ManagePanelTest -> 28/28 passed
- zenit-dev test --browser --class CmsAdminSmokeTest --class
  EnvironmentSecretsTest --class MigrationIntegrityTest -> 8/8 passed

Eight enumerated proof cases:
1. concurrent identical listeners -> twoSitesEnabling... (existing, still green)
2. all-vs-one overlap -> new counterfactual test (pre-fix fail recorded)
3. wildcard/exact shadowing -> new journey step 2 (+ PathRoutingTest wildcard)
4. paths -> PathRoutingTest (identical tuple refused, distinct paths route)
5. disabled sites -> twoSitesEnabling step 1 (staged duplicates legal, claim 0)
6. soft-deleted sites -> aDeletedSitesHostnameBecomesClaimableAgain (existing)
7. restore -> new journey steps 4-5
8. failed writes -> new journey steps 6-7 (rollback + retryable)

Known limitations or decisions:
- The overlap guarantee is engine-scoped by design: on a READ COMMITTED SQL
  backend the serialized-scan argument would not hold; hohenheim refuses
  non-SQLite at boot (requireSqlite), and the AIDEV-NOTE names this contract.
- Route-claim transactions must finish well under SQLite's 5s busy_timeout;
  current save paths are milliseconds (test barriers release within ~150ms).
- Schema hooks are permanent per JVM; both test barriers and the fault
  injector are volatile-toggled and cleared in @AfterEach.

Commit and push state:
hohenheim a19e1dd "Serialize route-claim scans and writes in one transaction"
(on master, NOT pushed). No framework repo required changes.
```