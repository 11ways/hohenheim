Both issues complete, proven, and committed. Final report:

```text
Issue: E9 — Ghostty script failure can hang terminal initialization forever
User-facing behavior changed: A pl-terminal whose ghostty-web script tag errored, or
  loaded without defining the global, used to hang silently forever (blank terminal,
  empty console). It now fails LOUDLY exactly once: one console.error
  "[pl-terminal] ghostty-web unavailable: <cause>", every queued init callback is
  settled exactly once with an IllegalStateException (same loud shape as the
  missing-tag branch), no script is ever injected, and the failure is sticky so later
  mounts fail immediately instead of re-queueing.
Architectural responsibility changed: none — the bridge still only AWAITS the
  template-declared tag (the :62-68 no-injection AIDEV-NOTE survives verbatim); the
  base CSP and the G8 asset pin are untouched.
Files changed (plumage):
  src/client/java/be/elevenways/plumage/component/BrowserTerminalBridge.java —
    awaitGhosttyWeb rewritten: terminal _initialized/_failed states; fail()/settle()
    no-op after either; per-callback try/catch drains (one throwing consumer cannot
    strand the queue); 'error' listener on the tag; already-settled-tag check via
    resource timing (an entry for existing.src only exists once the fetch finished;
    judgement deferred one macrotask so a just-finished healthy load still boots —
    boot() is _bootStarted-guarded so the two paths cannot race); WASM-init .catch and
    the no-init branch now route through fail(). New onFailed DataCallback parameter;
    Java side throws IllegalStateException naming the script() emission and the
    deliberate fallback removal.
  src/browserTest/templates/test/terminal-test.hwk — was a DEAD fixture (a tag
    declaration nothing instantiated; /component/terminal-test rendered an empty
    page). Rewritten as a top-level page with two pl-terminals (second init exercises
    the pending queue). Nothing else referenced the old shape.
Tests added or changed:
  TerminalFallbackTest.anErroredScriptTagFailsLoudlyInsteadOfHangingForever (route
    abort on the ghostty asset) and .aScriptThatLoadsWithoutTheGlobalFailsLoudly
    (route fulfill with global-less JS); both assert exactly ONE loud failure, zero
    canvases, zero injected scripts. Existing missingScriptTagFailsLoudly kept green.
Observed pre-fix failure (hang shape, run against the pre-fix bridge via git
  checkout + zenit-dev build --skip-deps, restored immediately; nothing reverted was
  published beyond the local skip-deps build):
    anErroredScriptTagFailsLoudlyInsteadOfHangingForever:
      "[step 1: ... console was: [error: Failed to load resource: net::ERR_FAILED,
      log: Starting Client-Size Zenit, error: Failed to load resource: the server
      responded with a status of 404 (Not Found)]] expected: 1L but was: 0L"
    aScriptThatLoadsWithoutTheGlobalFailsLoudly:
      "[step 1: ... console was: [log: Starting Client-Size Zenit]]
      expected: 1L but was: 0L"
  i.e. the bridge produced NO output at all — even the old "loaded but no init()"
  line never fired because the load event predated the late listener. Callbacks hung.
Verification command and result: plumage `zenit-dev test --browser
  TerminalFallbackTest --no-fail-fast` → 3/3 pass; TerminalTest 1/1, TerminalCspTest
  1/1 pass.
Backend/browser coverage: Chromium via Playwright (real page, real asset routes).
Known limitations or decisions: the already-settled-tag detection relies on the
  browser recording a resource-timing entry for the finished fetch (true in Chromium,
  proven by the abort test); a hypothetical browser recording none for an
  already-errored tag would fall back to hanging on that one path — no watchdog timer
  was added because a fixed timeout would falsely kill slow-but-healthy WASM loads.
Commit and push state: plumage 4a47471 (on top of 64e8f14), NOT pushed.
```

```text
Issue: F6 — pl-terminal never disposes on unmount
User-facing behavior changed: soft-navigating away from a page with a live terminal
  now closes its WebSocket, releases the fit addon and the ghostty instance, and
  removes all three container attachments; previously every terminal visit leaked a
  live socket + WASM terminal for the tab's lifetime. A remount boots exactly one new
  terminal on a fresh connection; an unmount DURING the first WASM load cancels the
  pending boot instead of leaking a terminal onto a dead element.
Architectural responsibility changed: none new — wired the already-existing
  Terminal.dispose (TerminalFunctions:117-131) through the existing Cleanup.on
  mechanism (CleanupFunctions:38-58, cited; pl-sheet is the reference consumer).
Keyed vs unkeyed: UNKEYED, deliberately — the registration site is @mount, which runs
  once per connected lifetime (a remount is a new lifetime and re-registers after the
  disposer already ran); the keyed form exists for registration sites that can repeat
  within one lifetime (@watch/event handlers), which this is not. Documented in the
  template comment.
Files changed (plumage):
  src/common/templates/components/terminal.hwk — Cleanup.on(this, () =>
    Terminal.dispose(containerEl)) directly beside Terminal.init.
  src/client/java/be/elevenways/plumage/component/BrowserTerminalBridge.java —
    disposeTerminal: detaches ws handlers BEFORE close (the async close event used to
    write into the already-disposed terminal), bumps a per-container
    DISPOSE_GENERATION attachment (always, even pre-boot), logs
    "[pl-terminal] disposed" after all attachments are removed; initTerminal's ready
    callback skips when the generation moved (boot cancel) or a terminal already
    exists (exactly-one guard). NOTE: an isConnected-based guard was tried first and
    is WRONG — @mount legitimately runs while soft-nav content is still detached, so
    the synchronous already-initialized ready path falsely skipped the remount; the
    generation tombstone is the deterministic signal (recorded in an AIDEV-NOTE).
  src/browserTest/... — spa-terminal-start.hwk / spa-terminal-away.hwk (extend the
    existing spa-layout he-block shell; real wsUrl=/ws/showcase-terminal),
    ShowcaseTerminalEndpoint gains OPENED/CLOSED AtomicInteger counters + onClose.
Tests added or changed: TerminalLifecycleTest.softNavigationDisposesTheTerminalAnd
  ARemountCreatesExactlyOne — one 4-step behaviour journey.
Proof assertions — which ran and where (all in plumage, all green):
  (a) browser WebSocket closes — step 2: the close is initiated by the browser-side
      disposer and observed server-side (delta CLOSED == 1 within 10s of soft-nav).
  (b) server handler detaches — same step 2 assertion: the echo handler's onClose is
      delivered through zenit's exactly-once WebSocketTeardown (e11d1ae), i.e. the
      handler release itself is what increments the counter.
  (c) attachments removed — step 3: "[pl-terminal] disposed" console line, emitted
      only after ws/fit-addon/terminal attachments were removed and released.
  (d) remount creates exactly one — step 4: delta OPENED == 2 with CLOSED still 1,
      and exactly one canvas under #spa-terminal after navigating back.
  Decision on the hohenheim half: NOT moved to hohenheim. "Server handler detaches"
  is a property of zenit's WebSocket teardown, proven here against a real
  WebSocketEndpoint in-process; ProcessTerminalHandler adds process semantics outside
  pl-terminal's contract. As the downstream consumer check, hohenheim (b772041) was
  instead rebuilt against the new plumage (full chain compile, 377s) and its
  ProcessTerminalHandlerTest run: 3/3 pass — no hohenheim commit needed.
Observed pre-fix failure (recorded before any fix existed):
  "TerminalLifecycleTest.softNavigationDisposesTheTerminalAndARemountCreatesExactlyOne()
   step 2: leaving the page must close the WebSocket and release the server handler
   exactly once: expected 1 but was 0" (10s await exhausted — the socket never closed).
Verification command and result: plumage `zenit-dev test --browser
  TerminalLifecycleTest --no-fail-fast` → 1/1 pass (1.8s); plus TerminalFallbackTest
  3/3, TerminalTest 1/1, TerminalCspTest 1/1; hohenheim `zenit-dev test --browser
  ProcessTerminalHandlerTest --no-fail-fast` → 3/3 pass.
Backend/browser coverage: Chromium/Playwright with a real in-process ZenitHttpServer
  and real WebSocket; hohenheim consumer suite on its SQLite runtime.
Known limitations or decisions: the "[pl-terminal] disposed" log is emitted only when
  something was actually released (a pre-boot cancel logs nothing — nothing existed);
  no user-visible strings added (console/error text is developer-facing, no microcopy
  needed; localization not applicable to this lifecycle mechanism).
Commit and push state: same plumage commit 4a47471 (both issues touch the same two
  production files, inseparable per ledger rule 16), NOT pushed. Hohenheim unchanged.
```

Incidental finding surfaced (fixed in passing, in the same commit): `terminal-test.hwk` was a dead fixture — a tag declaration nothing instantiated, so `/component/terminal-test` rendered an empty page; any test pointed at it would trivially pass or silently assert against nothing.