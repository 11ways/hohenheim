All work complete. Final report in the ledger's format.

---

```text
Issue: A3 -- Hawkeye compiler changes do not trigger Protoblast plugin repackaging
User-facing behavior changed: A hawkeye publish through zenit-dev is now automatically
  followed by a narrow `:protoblast-gradle-plugin:publishToMavenLocal` repackage of the
  plugin fat jar, BEFORE any later dependency or the consumer compiles with the plugin.
  Consumers can no longer run stale bundled compiler task code or hit the CompilerBridge
  version check after a hawkeye-only publish. A session that publishes hawkeye and dies
  before repackaging is healed by the next consumer build (pre-loop freshness check).
Architectural responsibility changed: The packaging edge is now OWNED by zenit-dev as a
  fingerprint edge (state key `<root>:protoblast#gradle-plugin`; identity = protoblast
  worktree fingerprint + hawkeye's PUBLISH-state fingerprint, deliberately not hawkeye's
  worktree, since packaging bundles the published hawkeye-compile jar). Cycle avoidance is
  responsibility separation: building hawkeye consumes the plugin as of the previous
  publish (bootstrap face); packaging follows every hawkeye publish (packaging face).
  CompilerBridge.REBUILD_HINT and hawkeye/CLAUDE.md no longer teach a manual order.
Files changed: zenit/tools/zenit-dev (pluginJarPath/pluginPackagingFingerprint/
  pluginRepackageStaleReason/recordPluginPackagingState/ensurePluginPackagingFresh; hooks
  in buildStaleDependencies pre-loop + after protoblast/hawkeye dep builds, doBuild force
  loop, doBuild self-build tail; also protoblast-gradle-plugin test-task routing in
  getGradleTestTask/testTaskForClass), zenit/tools/zenit-dev.test.js,
  hawkeye/hawkeye-compile/.../CompilerBridge.groovy (REBUILD_HINT), hawkeye/CLAUDE.md.
Tests added or changed: zenit-dev.test.js "a hawkeye change repackages the protoblast
  plugin fat jar before consumers build" (workspace fixture with a real
  protoblast-gradle-plugin subdir + a hook whose narrow-task invocation publishes ONLY the
  plugin jar, mirroring the real task): asserts order hawkeye -> repackage -> zenit ->
  fixapp, NARROW task only (no full protoblast rebuild), jar content actually changed,
  and convergence (third run rebuilds nothing).
Observed pre-fix failure: Against pre-fix zenit-dev (git stash) the test failed with
  "AssertionError: the plugin repackage must follow the hawkeye publish:" showing the log
  `hawkeye ... publishToMavenLocal / zenit ... / fixapp serverJar` with NO protoblast
  invocation -- consumers compiled with the stale plugin. Live drift also reproduced by
  recon (plugin jar 9h older than hawkeye-compile at session start).
Verification command and result: `node --test tools/zenit-dev.test.js` = 26/26 pass (25
  pre-existing + 1 new); the new test run 8/8 times to pin out a fixture flake (fake
  gradlew republished protoblast-server on the narrow task; hook fixed to mirror the real
  task). LIVE proof on the real workspace, twice: `zenit-dev build` in hawkeye printed
  "protoblast-gradle-plugin: hawkeye-compile republished since the plugin fat jar was
  packaged -- repackaging the fat jar"; journal `plugin.repackage reason=... duration
  13886ms`; the repackaged jar bundles the just-edited CompilerBridge (verified: new hint
  string present in the bundled .class, 99 hawkeye-compile entries).
Backend/browser coverage: n/a (orchestrator).
Known limitations or decisions: `--skip-deps` deliberately keeps its touch-nothing
  contract (except a protoblast self-publish records its own packaging); raw Gradle
  bypasses the edge -- both documented in the reworded REBUILD_HINT. I did NOT publish a
  reverted hawkeye-compile hunk to Maven local for a real-repo counterfactual (rule 15;
  the fixture counterfactual + live positive proof cover it, and other agents were
  actively building off the shared chain).
Commit and push state: zenit bd29474, hawkeye 1dcd1542 (docs/hint). Not pushed.
```

```text
Issue: A4 -- TeaVM classpaths can carry duplicate common/platform FQNs
User-facing behavior changed: Every configured TeaVM Generate* task now fails in doFirst
  (before any TeaVM compilation) when the same class FQN appears in two classpath
  entries. The concrete offenders are fixed: zenit-cms and zenit-media teavmLibs now
  carry ONLY org.teavm compiler jars (all be.elevenways client jars, including the
  fold-neutral hawkeye-common, were 100%-contained duplicates of browserTestJar --
  containment verified jar-by-jar, 0 missing classes each); zenit-auth drops
  plumage-client from teavmLibs for the same reason; hawkeye-core and textum drop the
  stacked common source-set output (compileBrowserJava already compiles common in the
  browser fold -- verified: 433/433 and 116/116 common classes present in browser
  output); textum's Generate task additionally filters the compile-only common dirs that
  ride browserCompileClasspath; arcana drops 4 client-jar repeats beside its fat clientJar.
Architectural responsibility changed: RuntimeClasspathGuard (protoblast-gradle-plugin)
  now covers TeaVM tasks, matched via the new package-visible
  ProtoblastGradlePlugin.isTeaVMGenerateTask class-name walk (mainClass-independent so
  late configuration cannot open a hole); classpath reached reflectively (no TeaVM
  compile dep). org/teavm/** is a DECLARED exemption: protoblast deliberately ships
  patched TeaVM classlib/jso classes (src/teavm/java/org/teavm) that shadow upstream
  jars by order -- the one sanctioned shadow lane, documented in the class AIDEV-NOTE.
Files changed: protoblast/protoblast-gradle-plugin/src/main/java/.../
  RuntimeClasspathGuard.java, ProtoblastGradlePlugin.java; zenit-cms/build.gradle;
  zenit-media/build.gradle; zenit-auth/build.gradle; hawkeye/hawkeye-core/build.gradle;
  textum/build.gradle; arcana/build.gradle.
Tests added or changed: ProtoblastPluginFunctionalTest.duplicateFqnOnTeaVMClasspathFails
  BeforeCompilation -- a real org.teavm 0.15.0 project whose duplicate entry is APPENDED
  (worst order: the copy that would silently lose the lottery), asserting the guard
  message, the offending FQN, and that no bundle file was produced.
Observed pre-fix failure: Fixture: `UnexpectedBuildSuccess ... with arguments
  [generateJavaScript]` -- the TeaVM compile ran to completion over a duplicate FQN
  (6.5s full compile pre-fix vs 972ms doFirst refusal post-fix). Real consumer: textum's
  first post-guard run failed with "Duplicate classes on the classpath of
  :generateBrowserTestJavaScript (116 total...) be.elevenways.textum.common.Textum in
  build/classes/java/browser / in build/classes/java/common" -- a live catch of the
  stacked-common duplicate, then fixed and green.
Verification command and result: zenit-dev browser test runs, all PASSED, each compiling
  a fresh bundle under the new guard: hawkeye CleanupDisposerBrowserTest (2), textum
  ReconcilerBrowserTest (1), zenit-media MediaDirectiveBrowserTest (3), zenit-cms
  PanelIndexBrowserTest (1), zenit-auth OidcLoginBrowserTest (2), zenit
  LocalePrefixBrowserTest (2), plumage AppShellTest (1). Emitted-JS inspection of both
  affected consumers' fresh bundles (zenit-media zenit-media-test.js, zenit-cms cms.js):
  server-only symbol "Running on Java" (protoblast common, !IS_TEAVM branch) = 0 hits;
  browser-fold branches of the previously-duplicated hawkeye COMMON classes present
  ("aria-activedescendant" NavigationFunctions, "data-starting-style" TransitionSupport)
  = 1 hit each. Static duplicate scans confirmed the remaining repos with TeaVM tasks
  (zenit, plumage, zenit-flow, quirkyquarters, spamservice/herald/thoth/proteus) have
  only org/teavm-exempt overlaps.
Backend/browser coverage: 7 repos' browser bundles compiled + Playwright-exercised.
Known limitations or decisions: arcana is not a git repository (edit uncommitted, not
  build-verified -- its removed lines are contained-by-construction in its fat
  clientJar); zenit-flow/quirkyquarters/spamservice/herald/thoth/proteus verified by
  static jar scans, not rebuilt. Identical-byte duplicates are still refused (FQN-once
  is the doctrine; hawkeye-client vs hawkeye-common were byte-identical TODAY but the
  hazard is the first common IS_TEAVM branch).
Commit and push state: protoblast 8552280, hawkeye f0b2982c, textum 047115a, zenit-cms
  4a058e7, zenit-media bf2d84f, zenit-auth 31e95e2. Not pushed.
```

```text
Issue: A7 -- Runtime duplicate guard ignores non-be.elevenways packages
User-facing behavior changed: The Test/JavaExec/TeaVM duplicate guard is
  package-independent: it scans EVERY .class entry (com.example generated apps included)
  instead of only be/elevenways/. Exempt: META-INF/** (multi-release copies),
  module-info.class, package-info.class (metadata, not code), and org/teavm/** (the
  sanctioned protoblast patch-shadow lane -- without this exemption every browser build
  in the workspace fails on protoblast's deliberate TUUID/TConcurrentHashMap/jso-event
  overrides of the upstream TeaVM jars). Failure message updated accordingly
  ("Duplicate classes on the classpath of <task>").
Architectural responsibility changed: One guard change shared with A4 (designed once):
  CLASS_PREFIX deleted, isExemptEntry() is the single exclusion definition for both the
  directory walk and the jar scan.
Files changed: RuntimeClasspathGuard.java (same commit as A4).
Tests added or changed: ProtoblastPluginFunctionalTest.duplicateFqnOutsideBeElevenways
  AlsoFailsTheBuild (com.example.dupsample clone of the existing fixture, via a shared
  writeDuplicateFqnProject helper); existing be.elevenways test updated to the new
  message and kept green.
Observed pre-fix failure: `UnexpectedBuildSuccess: Unexpected build execution success
  ... with arguments [test]` -- the com.example duplicate passed the pre-fix build
  (guard never enumerated outside be/elevenways/).
Verification command and result: `zenit-dev test --unit --class
  ProtoblastPluginFunctionalTest --no-fail-fast` = 8/8 PASSED. False-positive probes on
  the broadened scan: protoblast HTMLTokenizerTest (18 pass; classpath carries the
  org/teavm patch lane beside real teavm jars), zenit RouteLocalesTest (4 pass; the
  fattest third-party test classpath), plus every browser suite in the A4 list (their
  browserTest Test tasks are guarded too). No innocent third-party duplicate surfaced.
Backend/browser coverage: JVM Test tasks across protoblast/zenit + 7 browser suites.
Known limitations or decisions: A legitimately-duplicated third-party FQN in a repo not
  exercised here would now fail loudly by design; the failure names both entries and the
  fix belongs at the declaration.
Commit and push state: protoblast 8552280 (shared with A4/A8). Not pushed.
```

```text
Issue: A8 -- Functional tests depend on an obsolete Maven artifact
User-facing behavior changed: All three plugin functional-test classes
  (ProtoblastPluginFunctionalTest, ProtoblastApplicationDistFunctionalTest,
  BundleSizeBudgetFunctionalTest) now run against an ISOLATED Maven repo
  (protoblast/build/functional-test-repo) populated only by the current build: the root
  build gained a named `functionalTest` publishing repository, the plugin's test task
  dependsOn `:publishServerPublicationToFunctionalTestRepository` and threads the path
  via the `protoblast.functional.repo` system property. Fixtures dropped mavenLocal
  entirely (settings pluginManagement = gradlePluginPortal only, build repos = isolated
  repo + mavenCentral, cacheChangingModulesFor 0s), the plugin resolves via
  withPluginClasspath with a version-LESS plugins block (the current compile output, not
  the published fat jar), and the coordinate is the real publication
  `be.elevenways:protoblast-server:0.1.0-SNAPSHOT` instead of the retired
  `be.elevenways:protoblast:0.1.0-SNAPSHOT:server` classifier.
Architectural responsibility changed: Fixture artifact provenance is now build-owned;
  a stale ~/.m2 orphan can never keep an obsolete coordinate green again.
Files changed: protoblast/build.gradle, protoblast-gradle-plugin/build.gradle, the three
  test classes (shared settingsFor/buildPrelude/functionalRepo helpers).
Tests added or changed: All existing tests migrated onto the isolated-repo helpers.
Observed pre-fix failure: With the isolation in place and the OLD coordinate temporarily
  restored (proof-first inverse hunk, never published): `UnexpectedBuildFailure ...
  Could not find be.elevenways:protoblast:0.1.0-SNAPSHOT. Searched in ...
  functional-test-repo/be/elevenways/protoblast/0.1.0-SNAPSHOT/...` -- proving the old
  coordinate resolved ONLY from the 2026-06-26 mavenLocal orphan (jar date verified in
  recon; current publication is protoblast-server, protoblast/build.gradle:215).
Verification command and result: zenit-dev targeted runs: ProtoblastPluginFunctionalTest
  8/8, ProtoblastApplicationDistFunctionalTest 1/1, BundleSizeBudgetFunctionalTest 4/4,
  all PASSED. (Targeted runs required a zenit-dev routing addition: protoblast unit
  filters now route to `:protoblast-gradle-plugin:test` / `:test` exactly like the
  existing zenit-gradle-plugin doctrine -- committed with A3.)
Backend/browser coverage: n/a (Gradle TestKit).
Known limitations or decisions: SNAPSHOT publishes to the build-local repo accumulate
  timestamped artifacts under build/ (cleaned with the build dir). The plugin under test
  is the injected current classpath, not the published fat jar -- the fat-jar freshness
  question is A3's edge, now enforced by zenit-dev.
Commit and push state: protoblast 8552280 (shared commit). Not pushed.
```

Cross-cutting notes: repos protoblast/zenit/hawkeye/textum/zenit-cms/zenit-media/zenit-auth are committed and clean; arcana is not a git repository, so its build.gradle fix exists only as a worktree edit. The recon's claim that hawkeye-core's stacked source sets were "not a proven duplicate today" was false — `compileBrowserJava` compiles common sources into the browser output, so lines 189-190 were 433 real duplicate FQNs (guard caught the identical shape live in textum, 116 FQNs). No pushes were made anywhere.