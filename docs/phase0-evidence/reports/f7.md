All three worktrees are clean, all commits local (not pushed).

```text
Issue: F7 - Orcono editor lifecycle leaks across soft navigation (High)

Re-verification of the six sub-claims (all REAL, none falsified):
1. Static per-page state - REAL. EditorInitializer:170-186 held editorElement/activeProfile/
   entityId/baseRevision/mentionLabels/panelMentionsRef/pushingMentions as statics, plus
   :359-360 lastLabelRefreshAt/editorHasFocus.
2. Editor never destroyed - REAL. initialize:269 built a new TextumEditorElement per mount;
   no call to TextumEditorElement.destroy() existed anywhere in orcono.
3. Update-listener disposer discarded - REAL. setupMentionSync:489 dropped the Runnable that
   TextumEditor.registerUpdateListener returns (TextumEditor.java:376).
4. Synced-ref subscription never closed - REAL. setupLiveLabelSync:444-449 dropped both the
   SyncedRefSubscription (close() at SyncedRefSubscription.java:63) and the inner
   Ref.Subscription. Proven live: the server-side subscriber count climbed per navigation.
5. Panel bound exactly once, forever - REAL and the user-visible half. ensureMentionPanelBound:509
   early-returned on a non-null static ref, so after a soft nav the editor kept pushing into the
   PREVIOUS page's detached property-panel. The new page's sidebar stayed empty permanently.
6. Focus/click listeners never removed - REAL. setupLabelRefresh:372/386 and setupSaveButton:713
   added listeners without keeping identities. Measured: 2 focus listeners leaked per navigation.
   Additionally found while fixing: setupSaveButton/showSaveStatus queried the DOCUMENT, so during
   a soft nav where both pages coexist they could bind the OUTGOING page's button.

User-facing behavior changed:
  After soft-navigating between detail pages, editing @mentions updates the sidebar of the page
  you are actually looking at (previously only the first editor opened in a session ever worked;
  every page after it had a dead sidebar until a full reload). Leaving an editor now closes its
  live-rename channel subscription and releases its listeners and Textum instance.

Architectural responsibility changed:
  Per-editor state moved from EditorInitializer's statics into a new instance-scoped
  EditorSession owned by the editor custom element. EditorInitializer keeps only the seam
  install, the two profiles and the stateless typeahead factories. Sibling lookups
  (property-panel, save button, save status) are now scoped to the editor's own
  .page-container instead of the document, matching the doctrine the toolbar lookup already
  documented. Registration channel is the existing CustomElement.registerDisposer(key, ...)
  (hawkeye CustomElement.java:265), KEYED so a repeated mount disposes the previous generation
  instead of stacking; registered BEFORE start() so teardown always precedes fresh setup.
  Late async work (label refetch, save response, pushed rename delta, panel reconcile) is
  cancelled by an instance `disposed` flag, never by isConnected() - documented in an AIDEV-NOTE
  citing the plumage pl-terminal generation lesson.
  Dispose releases, in reverse order: save click listener, focusin+focusout, the inner
  Ref.Subscription, the SyncedRefSubscription, the panel Ref.Subscription, the update-listener
  Runnable, the toolbar, the Textum editor.

Files changed:
  orcono ecd8707: mvp-v01/src/client/java/be/elevenways/orcono/client/EditorSession.java (new),
    EditorInitializer.java, mvp-v01/src/browserTest/.../EditorSoftNavLifecycleBrowserTest.java (new)
  orcono 36ae266: mvp-v01/build.gradle
  textum 6ae80d5: src/browser/java/be/elevenways/textum/browser/element/TextumToolbar.java
    (new destroy(); the toolbar had no teardown API at all, and the ledger requires the toolbar
    be destroyed on unmount)
  zenit  f6a89b6: src/common/java/be/elevenways/zenit/common/channel/SyncedRefs.java
    (new subscriberCount(Identifier) - the only outside view of a leaked subscription)

Tests added or changed:
  New behaviour journey EditorSoftNavLifecycleBrowserTest.softNavigatingBetweenEditorsDisposes
  EachOneAndNeverTouchesADetachedPage. Real soft navigation only (search results -> page A ->
  back -> page B -> back -> issue C), with a window.__softNavProbe asserted alive at every step
  so a full reload cannot silently make the journey vacuous. A wrapper around
  EventTarget.addEventListener/removeEventListener counts LIVE focus listeners on
  .page-editor-content; SyncedRefs.subscriberCount is polled server-side. Anti-dead-fixture
  guard: before leaving page A the test asserts the captured panel HTML actually contains a
  mention row, and asserts window.__panelA.isConnected === false before relying on "detached".

  Ledger proof assertions actually run, and where:
   - constant listener counts across repeated navigation: EditorSoftNavLifecycleBrowserTest:84
     (2 after mount 1), :110 (0 after unmount), :124 (2 after mount 2), :144 (2 after mount 3).
   - constant resource counts: awaitCount on syncedRefSubscribers() at steps 1/2/3/4
     (baseline+1, baseline+0, baseline+1, baseline+1).
   - detached panel never changes: :131 and :138 compare window.__panelA.innerHTML to the
     snapshot taken on page A, after editing mentions on page B and on issue C.
   - each page's own sidebar answers its own edits: :89, :130, :149.

Observed pre-fix failure (two counterfactual runs, fix reverted in orcono only, --skip-deps,
nothing reverted published to Maven local):

  Run 1 (full test, pre-fix source):
    EditorSoftNavLifecycleBrowserTest.softNavigatingBetweenEditorsDisposesEachOneAndNeverTouchesADetachedPage()
      [step 2: leaving the editor must remove the focus listeners it added]
      org.opentest4j.AssertionFailedError: [step 2: leaving the editor must remove the focus listeners it added]
      expected: 0
      but was: 2
      at ...EditorSoftNavLifecycleBrowserTest.java:110
    (an earlier run of the same source, before I corrected the pair arithmetic, failed at step 1
     with "expected: 1 but was: 2", which is what revealed one editor binds a focusin AND a focusout)

  Run 2 (same pre-fix source, listener/subscription assertions temporarily neutralised so the
  journey could reach the panel assertions; the test file was restored byte-for-byte afterwards):
    org.opentest4j.AssertionFailedError: Locator expected to have count: 1
    Received: 0
    Call log: waiting for locator(".mention-relations .mention-relation")
    locator resolved to 0 elements
    unexpected value "0"
    at ...EditorSoftNavLifecycleBrowserTest.java:121
    -> that is step 3: page B's sidebar showed ZERO mention rows after inserting a chip, because
       the static panel ref still pointed at page A's detached panel. The user-visible defect.

Verification command and result:
  zenit-dev build (full chain) + zenit-dev build --skip-deps (orcono) - ok
  zenit-dev test --browser --class EditorSoftNavLifecycleBrowserTest -> PASSED, 1 browser passed
  zenit-dev test --no-fail-fast (orcono, whole suite) -> PASSED, 121 unit passed, 37 browser passed
  zenit-dev test --unit --class SyncedRefTest,ChannelProtocolTest (zenit) -> PASSED, 8 unit passed
  zenit-dev test --unit (textum) -> PASSED, 388 unit passed
  All runs via zenit-dev; no raw Gradle; pgrep checked and foreign builds waited on, never killed.

Blocker found and cleared on the way (report this, it is not F7):
  The orcono TeaVM build was broken before I could verify anything:
    Execution failed for task ':generateJavaScript'
    > Duplicate classes on the classpath of :generateJavaScript (3766 total, showing 15).
      be.elevenways.hawkeye.generated.tags.zenitcms.CmsActionSheet
        in .../orcono-mvp-0.1.0-SNAPSHOT-client.jar
        in .../zenit-cms-server-0.1.0-SNAPSHOT.jar
  Cause: orcono reused the name `teavmClasspath`, which the org.teavm plugin owns and extends
  from the main runtime classpath - and orcono extends that with every server dep, so the whole
  server stack was on the TeaVM input. Not caused by my change; exposed by protoblast 8552280's
  new TeaVM classpath guard. spamservice and thoth were already migrated this session to an
  app-owned `teavmInput` configuration with an explaining AIDEV-NOTE; I applied the identical
  pattern to orcono (commit 36ae266), including the five org.teavm runtime libs the plugin used
  to supply. Any other app still on the plugin-owned name will hit the same wall.

Backend/browser coverage:
  Browser: Chromium/Playwright against the real server on Testcontainers PostgreSQL (orcono is
  PostgreSQL-only). Both editor kinds exercised (page-editor and issue-editor). No datasource
  dimension is involved in this fix.

Known limitations or decisions:
  - TextumToolbar.destroy() detaches the toolbar subtree rather than removing listeners one by
    one; documented in an AIDEV-NOTE stating why no bookkeeping is needed (the buttons are
    created by the toolbar, referenced by nothing else, and the toolbar registers nothing on
    the editor). If a future toolbar registers an editor listener, it must keep that disposer.
  - initialize() now THROWS if the mount seam is handed something that is not a CustomElement,
    naming the contract, rather than silently leaking again. Both call sites pass `this` from a
    @mount hook, so this is unreachable today by construction.
  - EditorSession's `disposed` flag is per-session; there is no cross-session generation counter
    because there is no long async boot to cancel (unlike pl-terminal's ghostty load). If an
    async editor boot is ever introduced, the tombstone shape from plumage 4a47471 applies.
  - The listener probe counts .page-editor-content focus listeners only. The save-button click
    listener is now removed on dispose but is not independently counted by the test.
  - zenit's SyncedRefs.subscriberCount is a read-only observability accessor with one consumer
    (this proof). It is not exposed over any wire.

Commit and push state:
  orcono ecd8707, orcono 36ae266, textum 6ae80d5, zenit f6a89b6. All committed locally, all
  three worktrees clean, NOTHING pushed. zenit and textum were published to Maven local as part
  of the normal chain build (fixed source only - no reverted source was ever published; the
  counterfactuals reverted orcono alone and used --skip-deps).
```