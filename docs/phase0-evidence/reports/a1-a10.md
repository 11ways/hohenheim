Both issues are complete, proven, and committed. Final report:

```text
Issue: A1 — Stale Hawkeye generated classes survive tag removal or rename (High)
User-facing behavior changed:
  Generated template output is now an EXACT projection of the current template corpus.
  Removing a tag/class from a still-existing .hwk, renaming a tag, or deleting a
  template file deletes every stale generated .java/.sourcemap.json BEFORE compiling,
  and any such deletion forces a full recompile so dependant templates are re-validated.
  Previously the stale classes stayed in the output tree, compiled, were packed into
  build-cache entries, and shipped in published artifacts (behavior with no source).
  All four recon sub-claims re-verified true at HEAD before changing anything.
Architectural responsibility changed:
  The output post-condition grew an exactness half beside the completeness half:
  ProjectCompiler.reconcileOutput(Path) -> List<String> (deleted relative paths) is a
  new BRIDGED method; the allowed set = expectedGeneratedClasses (+ .sourcemap.json
  siblings) + the three always-regenerated auxiliary registration classes
  (HawkeyeClassSerializers / HawkeyeCustomElementRegistrations /
  HawkeyeDeclaredClassRegistrations, whitelisted so incremental runs are not
  misreported as stale); empty package dirs are pruned. HawkeyeCompile's F-14
  removed-file dir-wipe branch is replaced by this one mechanism: reconcile runs after
  scanAll, before compiling; forceFullRecompile = removedTemplates || staleRemoved ||
  hasTagDefinitionChanges. GRADLE_BRIDGE_VERSION 2 -> 3 and
  CompilerBridge.EXPECTED_BRIDGE_VERSION 2 -> 3 in the same commit; eager resolution +
  wrapper added. requireCompleteOutput's signature is untouched, so hawkeye-core's
  hand-rolled compileBrowserTestTemplates caller (which wipes its dir every run and is
  exact by construction) needed no change.
Files changed (hawkeye repo, commit 9cf4cd41):
  /home/skerit/projects/javaweb/hawkeye/hawkeye-core/src/server/java/be/elevenways/hawkeye/server/compiler/ProjectCompiler.java
  /home/skerit/projects/javaweb/hawkeye/hawkeye-core/src/server/java/be/elevenways/hawkeye/server/compiler/{HawkeyeClassCodeGenerator,CustomElementRegistrationGenerator,DeclaredClassRegistrationGenerator}.java (CLASS_NAME private -> package-visible)
  /home/skerit/projects/javaweb/hawkeye/hawkeye-compile/src/main/groovy/be/elevenways/hawkeye/compile/CompilerBridge.groovy
  /home/skerit/projects/javaweb/hawkeye/hawkeye-compile/src/main/groovy/be/elevenways/hawkeye/compile/HawkeyeCompile.groovy
Tests added or changed:
  New /home/skerit/projects/javaweb/hawkeye/hawkeye-core/src/test/java/be/elevenways/hawkeye/ExactOutputReconciliationTest.java
  (5 journeys: last-tag removal incl. the hasTagDefinitionChanges blind spot, tag
  rename old-FQN cleanup, template-file removal incl. sourcemap, exact-tree no-op +
  auxiliary/manifest preservation, cache pack/restore exactness).
Observed pre-fix failure (run 20260731-114144, 3 of 3 failed against untouched code):
  "step 3: the removed tag's interface must not survive in generated output ==> expected: <false> but was: <true>"
  "step 2: the old tag interface must not survive a rename ==> expected: <false> but was: <true>"
  "step 3: the deleted template's class must not survive in generated output ==> expected: <false> but was: <true>"
Verification command and result:
  zenit-dev test --unit --class ExactOutputReconciliationTest,RequireCompleteOutputTest,WriteTagSourcesTest --no-fail-fast (hawkeye) -> 8/8 PASSED (run 20260731-114404).
  Republish order executed (bridge bump): zenit-dev build in hawkeye, then
  zenit-dev build --force --skip-deps in protoblast (fat jar re-bundles bridge v3 —
  verified reconcileOutput present in both published jars), done TWICE (second pass
  after ASCII comment cleanup so artifacts match the committed source).
  Downstream check (risk: bridge v3 + reconcile flow inside the real plugin-driven
  task across a real consumer, and no spurious deletions/full recompiles):
  zenit-dev compile-templates in zenit -> full zenit build + template compile + javac
  green. Live 3-phase journey on zenit's real browserTemplates pipeline: probe tag
  compiled (ExactnessProbe/Impl/Tpl_ generated) -> tag removed with file kept,
  incremental run deleted all three stale tag classes while Tpl_ExactnessProbe stayed
  -> file deleted, template class gone; zenit worktree left clean.
Backend/browser coverage: n/a (build-time compiler mechanism; no datasource or browser surface).
Known limitations or decisions:
  Cache-restore proof is the journeyCacheRestoreOfAReconciledTreeStaysExact
  pack/restore simulation (copy of declared outputs post-action = Gradle's documented
  pack model, the same assumption requireCompleteOutput's design already rests on),
  not a TestKit fixture — a TestKit rig would have exercised the PUBLISHED plugin
  rather than the working tree (the A3 drift) and mostly re-tested Gradle itself.
  Consumers of a REMOVED tag fail at template compile ("unknown"/plain-element
  re-validation via the forced full recompile) or at javac — loud either way.
  A3 (plugin repackaging edge) remains open as ledgered; the manual hawkeye->protoblast
  order was executed here and is stated above.
Commit and push state: hawkeye 9cf4cd41 on type-system-v1, NOT pushed; worktree clean.

Issue: A10 — Stylesheet registration generation retains stale classes (Low)
User-facing behavior changed:
  Renaming hawkeye.namespace now removes the previous <Ns>StylesheetRegistration.java;
  before, the old @ZenitAutoLoad class stayed beside the new one (the output dir is a
  registered srcDir), kept compiling into published jars, and kept registering the
  retired stylesheet href at every consumer's boot. Claim re-verified at HEAD.
Architectural responsibility changed:
  GenerateStylesheetRegistrationTask now applies the exact-output doctrine to its
  @OutputDirectory: the task owns the whole directory, so generate() deletes it and
  regenerates from scratch (plain File ops — configuration-cache safe; the
  @OutputFile contribution was already overwrite-correct and is unchanged).
Files changed (zenit repo, commit efb7c6e):
  /home/skerit/projects/javaweb/zenit/zenit-gradle-plugin/src/main/groovy/be/elevenways/zenit/gradle/GenerateStylesheetRegistrationTask.groovy
  /home/skerit/projects/javaweb/zenit/zenit-gradle-plugin/src/test/java/be/elevenways/zenit/gradle/ZenitGradlePluginFunctionalTest.java
Tests added or changed:
  namespaceRenameRemovesTheStaleRegistrationClass (TestKit: runs
  generateZenitStylesheetRegistration twice with namespaces sample -> renamed, asserts
  RenamedStylesheetRegistration.java exists and SampleStylesheetRegistration.java is
  gone); writeFixture gained a namespace parameter and generatedSource(classPrefix),
  existing callers delegate unchanged.
Observed pre-fix failure (run 20260731-114015, against untouched task code):
  "step 2: the OLD namespace's registration class must be deleted, not left to register a retired stylesheet href ==> expected: <false> but was: <true>"
  (AssertionFailedError at ZenitGradlePluginFunctionalTest.java:97; 1 of 7 failed)
Verification command and result:
  zenit-dev test --unit --class ZenitGradlePluginFunctionalTest --no-fail-fast -> 7/7
  PASSED post-fix, including generationReusesConfigurationCache (UP-TO-DATE runs do not
  execute, so the generated file survives up-to-date/config-cache reuse) and the
  contribution-wiring tests.
Backend/browser coverage: n/a (Gradle plugin task).
Known limitations or decisions: none; deletion is bounded to the plugin-owned
  build/generated-sources/zenit-stylesheets/java directory.
Commit and push state: zenit efb7c6e on master (on top of the prior agent's d62ef3b;
  tools/zenit-dev untouched), NOT pushed; worktree clean.
```

Republish order executed for the bridge bump: hawkeye first (publishes hawkeye-server + hawkeye-compile with bridge v3), then protoblast with `--force --skip-deps` (repackages the plugin fat jar), then the zenit downstream verification — performed twice, with the final pass matching the committed source exactly.