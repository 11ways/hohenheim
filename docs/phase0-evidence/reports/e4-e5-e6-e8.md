All work is complete and committed. Final report:

---

## BLOCKER FOUND AND FIXED FIRST

**zenit did not compile at HEAD (e11d1ae, the E2 commit).** `WebSocketTeardown.java:45,51` wrote `@NonNull ZenitHttpServer.SerialExecutor` — a type annotation on a qualified nested type, which javac rejects (`error: type annotation @org.checkerframework.checker.nullness.qual.NonNull is not expected here`, x2). Corrected to `ZenitHttpServer.@NonNull SerialExecutor` in both places. Nothing else in that file was touched; the exactly-once teardown guarantee is untouched. Whoever ran E2 verified with a stale artifact.

---

```text
Issue: E4 - Contextual route parameters execute before authorization
User-facing behavior changed: A request to an endpoint declaring requiresLogin/requiresPermission whose route carries a context-resolved parameter (ModelParam) is now refused 401/403 with ZERO database queries. Previously the resolver queried during matching, i.e. before any middleware. An anonymous caller now receives the identical answer for an existing and a non-existing record; before, an existing row produced 401 and a missing row fell through to whatever public endpoint matched next (a 404 or a fallback page) - a record-existence oracle. Ungated endpoints are behaviorally unchanged, fall-through included. On the WebSocket path, upgrade matching no longer resolves at all before the method check, Origin, admission and the authenticator.
Architectural responsibility changed: Route matching is split into a CHEAP STRUCTURAL phase and a CONTEXTUAL RESOLUTION phase. New seam: RouteMatchContext.deferringContextual()/resolvingContextual()/isContextualDeferred(), EndpointRoute.MatchResult.hasDeferredParameters()/resolveDeferredParameters(ctx), ParameterDefinition.hasContextResolver(). WHO defers is decided by the endpoint's own declared gate (Endpoint.getParameterMatches), so the endpoint that owns the authorization requirement owns the deferral. WebSocketEndpoint.getParameterMatches is now unconditionally structural (it runs before every gate). The 405 method-collection walk is structural too.
Files changed: zenit common/routing/{RouteMatchContext,ParameterDefinition,EndpointRoute,Endpoint,WebSocketEndpoint,ModelParam}.java; server/http/{HttpConduit,ZenitHttpServer}.java; zenit/CLAUDE.md.
Tests added or changed: AdmissionRateLimitHttpTest.aGuardedContextualParameterResolvesOnlyAfterAuthorization (4-step journey over the real server, counting datasource reads); WebSocketAdmissionHttpTest.contextualParametersResolveOnlyAfterTheHandshakeGates (5-step journey: wrong method / cross-origin / anonymous-existing vs anonymous-missing / authorized-resolves-once / authorized-missing = 404).
Observed pre-fix failure:
  AdmissionRateLimitHttpTest.aGuardedContextualParameterResolvesOnlyAfterAuthorization()
    [step 1: refusing must cost no database query at all]
    org.opentest4j.AssertionFailedError: [step 1: refusing must cost no database query at all]
    expected: 0
    but was: 1
  WebSocketAdmissionHttpTest.contextualParametersResolveOnlyAfterTheHandshakeGates()
    [step 1: a wrong-method upgrade must not resolve a contextual parameter]
    org.opentest4j.AssertionFailedError: [step 1: a wrong-method upgrade must not resolve a contextual parameter]
    expected: 0
    but was: 1
Verification command and result: zenit-dev test --unit --class <16 routing/http/setting classes> -> 80 passed. zenit-dev test --unit --no-fail-fast (full zenit) -> 2039 tests, 5 failures, ALL environmental: 4 Couchbase testcontainer ServerOutOfMemoryException ("No memory available to store item", ENOMEM) and ChannelProtocolTest.broadcastReachesEveryOpenLink TimeoutException, which passes on its own (re-run: 3 passed) - a flake under the OOMing container load.
Backend/browser coverage: SQLite (the ModelParam fixture datasource). No browser surface; client-side matching already returns null for ModelParam (Blast.isServer guard), unchanged.
Known limitations or decisions:
  - 404 semantics without an oracle: preserved by making the ANSWER identical, not by preserving the mechanism. Anonymous -> 401/403 before any query, identically for existing and missing records. Authorized -> the row loads and a miss is a plain 404. The old "fall through to a competing endpoint" was itself the oracle (existing = 401, missing = the public fallback's answer), so it is deliberately dropped FOR GATED ENDPOINTS ONLY. AdmissionRateLimitHttpTest.missingModelRowFallsThroughToCompetingCandidate (the ungated case) is still green and unchanged.
  - The WS half was LATENT, not live: ModelParam has zero production consumers anywhere in the workspace (grep over javaweb + hohenheim: only ParameterDefinition, ModelParam itself and three test classes). The HTTP half is equally latent today by that same fact. I fixed both because the mechanism is shipped and documented as usable, and because E6's exact-match claim needed the structural-match seam anyway.
  - New constraint, documented in the AIDEV-NOTE and in zenit/CLAUDE.md: a matchWhen guard on a GATED endpoint must not read a context-resolved parameter. No consumer does (zenit-cms panelSlugIsRegistered reads a plain stringResolver param).
  - The WS authenticator receives structural parameters only (a peer's identity must not depend on a database-loaded route row). The handshake is rebuilt with resolved parameters only when deferral actually happened.
Commit and push state: zenit 9fc2846. NOT pushed.
```

```text
Issue: E5 - Bad-Origin WebSocket requests bypass admission while doing synchronous diagnostics
User-facing behavior changed: A cross-origin upgrade flood still gets 403 for every single attempt (never damped), but the diagnostics behind those refusals - the http.websocket.origin_refused log line and the synchronous SecurityEvents fan-out - are bounded to 10 per client IP per 60s.
Architectural responsibility changed: Option chosen: A CHEAP INDEPENDENT REJECTION LIMITER, not async sinks. New WebSocketAdmission.allowRefusalDiagnostics(clientIp), riding the existing AdmissionRateLimiter bounded bucket store under its own key family "ws_refusal_diagnostics_ip" - the same "one max_buckets budget, prefixed families" convention WebSocketAdmission already documents. Async sinks were rejected: they would have moved an unbounded queue behind the same unbounded producer and changed the semantics of every sink for one caller's benefit.
Files changed: zenit server/http/WebSocketAdmission.java (new limiter + AIDEV-NOTE), server/http/ZenitHttpServer.java (Origin refusal gated; the :491-498 AIDEV-NOTE extended, not removed), zenit/CLAUDE.md.
Tests added or changed: WebSocketAdmissionHttpTest.crossOriginFloodCannotAmplifyRefusalDiagnostics (30 attempts vs a cap of 10). WebSocketAuthHttpTest.refusedUpgradesReportSecurityEvents gained one line of hygiene (AdmissionRateLimiter.limiter().clear() at entry) because it asserts an EXACT event count and every class in the JVM refuses from 127.0.0.1.
Observed pre-fix failure:
  WebSocketAdmissionHttpTest.crossOriginFloodCannotAmplifyRefusalDiagnostics()
    [step 2: 30 refusals must not produce 30 security events]
    java.lang.AssertionError: [step 2: 30 refusals must not produce 30 security events]
    Expecting size of:
    [SecurityEvent[type=ws.origin_refused, remoteIp=127.0.0.1, ...], ... x30]
Verification command and result: post-fix WebSocketAdmissionHttpTest 6 passed, WebSocketAuthHttpTest 5 passed.
Backend/browser coverage: n/a (transport layer).
Known limitations or decisions:
  - crossOriginHandshakesDoNotConsumeTheAdmissionBudget STILL HOLDS and is asserted twice: it passes unchanged, and my new test re-pins it as its own step 3 (AUTH_CALLS zero after the flood, both of the victim's handshakes still admitted, the 3rd still 429). The diagnostics bucket is a separate key family, so spending it cannot touch the handshake budget.
  - No new settings. The cap is two constants (REFUSAL_DIAGNOSTICS_REQUESTS=10 / _WINDOW_SECONDS=60). Adding three settings knobs for a diagnostic damper would be scaffolding; the operator still sees the first 10 refusals per IP per minute, which is enough to diagnose a legitimate misconfigured origin.
  - Applied to the Origin refusal only. The 429 and 401/403 refusals already sit BEHIND admission, so they are bounded by construction.
Commit and push state: zenit 9fc2846. NOT pushed.
```

```text
Issue: E6 - claimingRoutesOf is incomplete AND overbroad
User-facing behavior changed: A module's scoped CSP now covers exactly the responses it serves. Its per-locale route variants (/aanmelden next to /login) get the policy where they previously got none at all; a host route that merely lives under a module route path (/login/host-owned) no longer gets the module's policy stamped on it.
Architectural responsibility changed: The claim is derived by running the REAL route matcher (EndpointRoute.getParameterMatches) against the claim path, over endpoint.getRoutes(null) - which yields the default routes AND every locale variant - instead of hand-deriving a leading static prefix and claiming its subtree. It runs with RouteMatchContext.none().deferringContextual(), so a claim check can never trigger a ModelParam query. This is E4's structural-match seam paying for itself. claimsRoute() is deleted; no parallel matcher survives.
Files changed: zenit server/http/ScopedCspMiddleware.java, zenit/CLAUDE.md.
Tests added or changed: ScopedCspHttpTest.derivedNamespaceClaimsMatchRoutesExactly (5-step journey with new fixtures: a module namespace with a static route, a dynamic-segment route, an nl locale variant, plus a host endpoint under the module's path).
Observed pre-fix failure (both halves, two counterfactual runs):
  incompleteness -
    ScopedCspHttpTest.derivedNamespaceClaimsMatchRoutesExactly()
      [step 2: a declared locale route variant must be claimed]
      org.opentest4j.AssertionFailedError: [step 2: a declared locale route variant must be claimed]
      Expecting value to be true but was false
  over-claim -
    ScopedCspHttpTest.derivedNamespaceClaimsMatchRoutesExactly()
      [step 2: a foreign route under the module's path must not be claimed]   (step 3 after the reorder)
      org.opentest4j.AssertionFailedError: Expecting value to be false but was true
Verification command and result: ScopedCspHttpTest 8 passed. Downstream zenit-auth: full unit suite 152 passed, and the REGRESSION ANCHOR run explicitly - AuthCmsResourcesIntegrationTest 9 passed, WebSocketAuthIntegrationTest 5 passed, AuthRegistryTest 4 passed. claimingRoutesOf("zenitauth").test("admin/users")==false and .test("login")==true are both still asserted and green.
Backend/browser coverage: n/a.
Known limitations or decisions:
  - Collision diagnostics untouched: reportOverlap + LOG.warning and the WEIGHT/VARIANT_WEIGHT tiering are unchanged; overlappingClaimsAreReportedLoudly and terminalConcessionsRideOnlyTheTerminalRoute are green.
  - Named downstream risk: zenit-auth is the only production consumer. Its routes are fully static except /login/{provider_slug}/start|callback, which uses a plain stringResolver - matched exactly, still claimed. A 404 inside a module's path subtree no longer gets the module's CSP; it renders no module content, so that is correct, not a loss.
  - Slightly more work per request (a real match per route in the namespace, vs building a prefix string). Same order of magnitude; the per-request registry walk stays deliberate, per the existing AIDEV-NOTE.
Commit and push state: zenit 9fc2846. NOT pushed.
```

```text
Issue: E8 - Non-positive global revalidation interval silently opts out
User-facing behavior changed: network.websocket_revalidation_interval_ms = 0 or a negative can no longer switch identity revalidation off for the whole process. It is refused on the load path (file/env/settings editor), throws on programmatic setValue, and is ignored (with a distinct problem log) if it somehow got past both.
Architectural responsibility changed: The setting is now a CADENCE knob with a positive-only contract, matching WebSocketEndpoint.revalidateEvery's existing "Revalidation interval must be positive". Opting out is exclusively the endpoint declaration neverRevalidate(). Three guards, one contract, deliberately: a coercer (SettingDefinition.Builder.coercer, the network.trusted_proxies prior art, reusing PrimitiveCoercion.toLong so the numeric parsing is not re-implemented), a listener (setValue does NOT run coercers - the same coercer+listener pairing trusted_proxies uses), and a read-side refusal in WebSocketRevalidator.intervalFor, which replaced the Math.max(0L, ...) clamp that WAS the silent opt-out. New public constant WebSocketRevalidator.DEFAULT_INTERVAL_MS.
Files changed: zenit server/setting/ServerSettings.java, server/http/WebSocketRevalidator.java, zenit/CLAUDE.md.
Tests added or changed: WebSocketRevalidationHttpTest.aNonPositiveGlobalIntervalCannotDisableRevalidation - a 6-step journey covering ZERO (load + programmatic), NEGATIVE (string-shaped "-1" too), a positive control, the runtime refusal, the ENDPOINT OVERRIDE (new NEVER fixture with neverRevalidate() -> 0) and ANONYMOUS SOCKETS (UNGATED with a null principal -> 0). Two new endpoint fixtures: SETTING_DRIVEN (gated, no declared interval) and NEVER.
Observed pre-fix failure:
  WebSocketRevalidationHttpTest.aNonPositiveGlobalIntervalCannotDisableRevalidation()
    Expecting code to raise a throwable.
    java.lang.AssertionError:
    Expecting code to raise a throwable.
    at ...WebSocketRevalidationHttpTest.aNonPositiveGlobalIntervalCannotDisableRevalidation(WebSocketRevalidationHttpTest.java:487)
Verification command and result: WebSocketRevalidationHttpTest 11 passed, ChannelGatewayRevalidationHttpTest 1 passed (the only other consumer of this setting; it sets 100L, still legal), SettingTests 7 passed, SettingsEditorTest 4 passed.
Backend/browser coverage: n/a.
Known limitations or decisions:
  - Named downstream risk: this is a setting CONTRACT change. A deployment with the key set to 0 will now FAIL SETTINGS LOADING loudly instead of booting with revalidation off - the same posture network.trusted_proxies already established for an invalid CIDR, and the correct one for a security mechanism. Repo-wide grep: no app sets this key; only ChannelGatewayRevalidationHttpTest (100L) and the definition itself.
  - A throwing listener runs AFTER SettingsContext.swap, so a programmatic setValue(0) stores the value before throwing (a pre-existing property of the trusted_proxies pattern, not introduced here). That is precisely why the read-side guard exists rather than being belt-and-braces: a swallowed exception cannot disable revalidation.
Commit and push state: zenit 9fc2846. NOT pushed.
```

**Cross-cutting verification:** zenit full unit suite (2039 tests, 5m03s) - the 5 failures are 4x Couchbase container ENOMEM plus one ChannelProtocolTest timeout that passes in isolation; none touch the changed code. zenit-auth 152 passed + anchors re-run explicitly (18 passed). zenit-cms 533 passed (named risk: all generated CMS routes are gated and carry matchWhen guards, so they are the largest population exercising the new structural-match path; none carry contextual parameters, so no deferral engages).

**Counterfactual method:** each fix was inverted with a minimal inverse hunk while keeping new API symbols so the tests compiled, run with `--skip-deps` (nothing reverted was ever published to Maven local), then restored byte-for-byte from a backup and re-verified (`grep COUNTERFACTUAL` over src: clean).

**Not pushed. One commit: zenit `9fc2846`.** No other repository was modified.