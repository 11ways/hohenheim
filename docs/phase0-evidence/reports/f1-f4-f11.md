All done and verified. Final report:

---

**Issue: F1 — List directives erase independently authored disabled state**
- User-facing behavior changed: a control disabled by business policy no longer becomes clickable when the list boundary allows the action. `use:List.remove` no longer strips authored `disabled` (previously it ALWAYS did); `use:List.moveUp/moveDown` preserve authored `disabled` when the boundary relaxes.
- Architectural responsibility changed: `ListDirectives.applyRenderState` now records directive ownership in a new marker attribute `data-list-disabled` (value `author` when the author had disabled it first, since attributes apply before directives); relaxing removes only directive-applied state. Mirrors the existing per-directive marker + aria-label-precedence pattern in the same file.
- Files changed: `hawkeye/hawkeye-core/src/common/java/be/elevenways/hawkeye/common/directive/ListDirectives.java` (constants `DISABLED_MARKER`/`AUTHOR_ALSO_DISABLED`, composed guard, AIDEV-NOTE), `.../src/browserTest/resources/templates/directive-list-test.hwk` (policy-disabled + reactive-locked rows).
- Tests added or changed: `ListDirectivesRenderTest.anAuthorSuppliedDisabledSurvivesAnAllowedBoundary`, `.boundaryOwnedDisabledIsMarkedDirectiveOwned` (server SSR); `ListDirectiveBrowserTest.authoredDisabledAndBoundaryDisabledChangeIndependently` (5-step journey: policy+boundary compose at SSR, boundary relaxes via reorder while policy survives, directive re-run leaves reactive policy-disabled state, policy toggles independently, remove works after unlock).
- Observed pre-fix failure: `org.opentest4j.AssertionFailedError: author-supplied disabled survives use:List.remove: <button id="r0" data-list-remove="" aria-label="Remove"> ==> expected: <true> but was: <false>` and `the boundary guard records that the DIRECTIVE applied disabled: <button id="u0" data-list-move-up="" disabled="" aria-label="Move up"> ==> expected: <true> but was: <false>`.
- Verification: `zenit-dev test --unit --class ListDirectivesRenderTest` (5 pass), `zenit-dev test --browser ListDirectiveBrowserTest` (2 pass).
- Backend/browser coverage: JVM SSR + Chromium hydration/reactive re-run. Consumer suites (rendered-output risk): plumage `PermissionsEditorTest` (2 pass), zenit-cms `ListViewsBrowserTest` (2 pass), quirkyquarters `CharacterAdminTest` (8 pass); no consumer authors `disabled` on a `use:List.*` control (grepped).
- Known limitations: one attribute cannot carry two live writers — if the author's REACTIVE lane sets `disabled` after the directive claimed ownership, a later boundary relax still removes it (documented in the AIDEV-NOTE). Full OR-composition would need a second channel; not required by the ledger.
- Commit and push state: hawkeye `6c443d84`, not pushed.

**Issue: F2 — `attr:on*` bypasses the inline-handler compile error**
- User-facing behavior changed: `attr:onclick="..."` (any DOM handler name in the `attr:` lane) is now the same `inline-event-attribute` compile ERROR as the plain spelling; `attr:once` and other non-handler names stay legal (closed `INLINE_HANDLER_ATTRIBUTES` set unchanged).
- Architectural responsibility changed: `checkInlineEventAttributes` judges the effective DOM attribute name — copied the `!"attr".equals(namespace)` allowance already used by `checkRetiredAttributes` in the same file.
- Files changed: `hawkeye/hawkeye-core/src/server/java/be/elevenways/hawkeye/server/analysis/TemplateWiringAdvisor.java` (one condition + comment), `hawkeye/CLAUDE.md` (diagnostics table row).
- Tests: `InlineHandlerDiagnosticTest.anAttrNamespacedClickHandlerFailsTheBuildByDefault`, `.anAttrNamespacedBodyOnloadFailsTheBuildByDefault`; `nonHandlerNamesStartingWithOnStayQuiet` extended with `attr:once`; `eventNamespaceBindingsStayQuiet` still green.
- Observed pre-fix failure: `org.opentest4j.AssertionFailedError: Expected java.lang.Exception to be thrown, but nothing was thrown.` (both attr: cases compiled clean).
- Verification: `zenit-dev test --unit --class InlineHandlerDiagnosticTest` (8 pass).
- Downstream sweep: grep of all `.hwk` in javaweb + hohenext for `attr:on` — ZERO occurrences; full template corpora recompiled green (zenit, plumage, zenit-forms, zenit-cms, hohenheim builds; quirkyquarters browser-test compile during its suite).
- Commit: hawkeye `01c63dec`, not pushed.

**Issue: F3 — directive synthetic methods have no source marker**
- User-facing behavior changed: exceptions thrown inside directive event listeners, reactive directive render methods, and typed-form submit listeners now translate to the AUTHORED template line instead of the prior method's last marker.
- Architectural responsibility changed: reused `IRTranspiler.emitSourceMarker(IRUnit)` immediately after `useMethodBuilder` in all three synthetic-method lanes; no new mechanism.
- Files changed: `DirectiveTranspiler.java` (event lane + reactive lane), `FormActionTranspiler.java` (submit lane — the flagged `:109` sibling, FIXED, anchored on the ACTION attribute, see finding below), testSupport `TestEventDirectives.java` (throwing `TestDir.explode` render fixture).
- Tests: `SourceMapTest.aThrowFromAReactiveDirectiveMethodMapsToTheAuthoredLine` (real render-time throw, translated via `SourceMapRegistry.translateException`, asserts `directive_throw_map:5`), `.anEventDirectiveListenerBodyMapsToTheAuthoredLine`, `.aTypedFormSubmitListenerBodyMapsToTheAuthoredLine` (runtime-lookup mapping of the listener call line; both templates plant a later `{% let %}` marker so a marker-less listener provably resolves wrong).
- Observed pre-fix failures: reactive lane — frame `at ...Tpl_DirectiveThrowMap.directive_1(Tpl_DirectiveThrowMap.java:117)` carried NO template annotation and the trace annotated only `renderRoot` with `template: directive_throw_map:4` (assert on `:5` failed `expected: <true> but was: <false>`); event lane — `the directive call inside the listener method maps to the authored line 3 ==> expected: <3> but was: <4>`; form lane — `the submission begin inside the listener method maps to the authored line 3 ==> expected: <3> but was: <4>` (captured via a temporary revert run with `--skip-deps`, sources restored immediately, nothing reverted was published).
- Verification: `zenit-dev test --unit --class SourceMapTest` (11 pass), plus `DirectiveTranspilerTest` (42), `EventDirectiveTranspilerTest` (6), `FormActionTest` (38), `FormActionCsrfTest` (6), browser `EventDirectiveTest` (6), `FormActionBrowserTest` (21).
- Commits: hawkeye `d7a71b35` (directive lanes) and `c7903b19` (form lane), not pushed.

**Issue: F4 — retired attributes are case-sensitive**
- User-facing behavior changed: `DATA-CONFIRM` / any case spelling of a retired attribute is now the same `retired-attribute` compile ERROR as the registered lowercase name; the diagnostic reports the AUTHORED spelling.
- Architectural responsibility changed: `RetiredAttributeRegistry` normalizes keys lowercase on `register` and `lookup`; `Retirement.name()` keeps the declared spelling. Non-HTML namespaces unaffected (the advisor only feeds it plain and `attr:` lanes).
- Files changed: `RetiredAttributeRegistry.java`, `hawkeye/CLAUDE.md` (table row).
- Tests: `RetiredAttributeTest.aRetiredAttributeIsCaseInsensitiveAndReportsTheAuthoredSpelling`, `.mixedCaseSpellingsFailTheBuildByDefault`; `commentsMentioningTheNameStayLegal` and `unrelatedAttributesStayLegal` still green.
- Observed pre-fix failure: `java.lang.AssertionError: the uppercase spelling bypassed the retirement: []` and `Expected java.lang.Exception to be thrown, but nothing was thrown.`
- Downstream sweep: case-insensitive grep for `data-confirm` across javaweb + hohenext `.hwk` — only a prose comment in zenit-cms `shell.hwk:86`; the real declaration (`CmsConfirmFunctions.java:35`) unchanged; zenit-cms + hohenheim recompiled green.
- Commit: hawkeye `4d05e77f`, not pushed.

**Issue: F11 — PathProbe hand-written platform holder**
- User-facing behavior changed: none (architecture refactor); browser/probe-less default remains UNKNOWN and existence checks still never block there.
- Architectural responsibility changed: `PathProbe.HOLDER` + static `get()`/`install()` DELETED; replaced by `PlatformSeam<PathProbe> SEAM = PlatformSeam.withDefault(...)` (exact `ZoneClock`/`DateBridge` shape). `JvmPathProbe` keeps `@ZenitAutoLoad` + `LOADED = install()` repointed to `SEAM.install`; `FilesystemPath` reads via `SEAM.require()` (never throws for withDefault seams — `PlatformSeamTest` pins semantics).
- Files changed: `zenit/src/common/.../validation/PathProbe.java`, `.../validation/validator/FilesystemPath.java`, `zenit/src/server/.../validation/JvmPathProbe.java`, `zenit/src/test/.../edit/PathFieldTest.java`. `PathField.java` needed no code change (doc references only). Workspace grep confirms zero remaining `PathProbe.get/install/HOLDER` references.
- Observed pre-fix failure (counterfactual for a pure refactor): the seam-based test written first failed compilation against the holder API — `PathFieldTest.java:37: error: cannot find symbol - PathProbe.SEAM`.
- Verification: `zenit-dev test --unit --class PathFieldTest` (7 pass, including `probelessEnvironmentsNeverBlockOnExistence` and real-filesystem probe checks); full `zenit-dev build` of zenit green including the TeaVM client fold.
- Commit: zenit `2c7d8fb`, not pushed.

**Compiler bridge / packaging edge:** no bridged signature changed, so `GRADLE_BRIDGE_VERSION`/`EXPECTED_BRIDGE_VERSION` were NOT bumped (stay v3). Both hawkeye publishes triggered the automatic protoblast-gradle-plugin repackage — observed in zenit-dev output: "protoblast-gradle-plugin: hawkeye-compile republished since the plugin fat jar was packaged -- repackaging the fat jar / ok protoblast-gradle-plugin".

**Chain verification:** after the final hawkeye publish, full builds green: zenit (80s), plumage (79s), zenit-forms (71s), zenit-cms (328s, chain rebuild), hohenheim (261s). No new diagnostics fired anywhere (only pre-existing a11y warnings).

**FormActionTranspiler finding (mandated report):** confirmed at `FormActionTranspiler.java:109` — identical marker omission; fixed in hawkeye `c7903b19` with its own counterfactual (`expected: <3> but was: <4>`).

**New adjacent defect discovered, NOT fixed (report only):** `IRElement` start offsets include the element's preceding whitespace text node, so EXISTING element-open source markers anchor one line early whenever an element follows a newline (observed in generated output: `<p>` on template line 2 marked `@hwk:1`, `<form>` on line 3 marked `@hwk:2`; an element with no preceding text emits no marker at all). This predates this work, affects every element mapping by one line under the preceding-marker semantics, and fixing it is a positions sweep with its own proof burden — that is why the form-lane marker anchors on the action ATTRIBUTE (whose offsets are correct, as are `IRDirective` offsets, proven by the `:5` runtime assertion).

**Commit and push state:** hawkeye `01c63dec`, `4d05e77f`, `d7a71b35`, `6c443d84`, `c7903b19`; zenit `2c7d8fb`. Both worktrees clean. Nothing pushed.