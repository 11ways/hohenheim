All three issues fixed, proven, and committed in hohenheim only. No framework change was required.

```text
Issue: E3 - In-band dev-tunnel authentication never revalidates
User-facing behavior changed: A live dev tunnel is now closed (1008) within one
  revalidation tick when its authorization stops holding: the namespace site's
  registration token is rotated, the site is disabled, soft-deleted, retyped away
  from dev-namespace, or its wildcard domain is withdrawn/renamed. Previously the
  tunnel stayed up until the client or transport ended it.
Architectural responsibility changed: THIN WIRING, no generic seam. Justification:
  DEV_TUNNEL is the only in-band-authenticated WS endpoint in either repo, and the
  seam the ledger would have had me build ALREADY EXISTS -- WebSocketHandler.revalidate()
  (exercised by zenit's /ws-reval-hooked fixture). What was missing was not a
  mechanism but a DECLARATION: WebSocketRevalidator.intervalFor's identity test
  (requiresAuthorization || non-anonymous handshake principal) is false for an
  anonymous handshake, so ZenitHttpServer created no revalidator at all and the
  existing hook was never reached. Declaring .revalidateEvery(...) makes intervalFor
  return the declared interval regardless of principal, which starts the revalidator
  and reaches the hook. Second change: findNamespaceSite now matches a sha256 DIGEST
  (SecureTokens.sha256Hex + constantTimeEquals, no early break preserved) instead of
  the plaintext token, so revalidation can re-run the SAME resolution registration
  used without the connection retaining its registration secret for its lifetime.
  Authorization changes that now close a live tunnel: token rotation, site disable
  (findEnabled filters ENABLED), site soft-delete (findEnabled filters DELETED_AT),
  site type change away from dev-namespace, wildcard-domain removal/rename (origin
  mismatch). Ownership/grant changes do NOT close it, because the tunnel's
  authorization is token-to-site and never consulted a principal - stated as a
  limitation, not silently implied.
Files changed:
  /home/skerit/projects/hohenext/hohenheim/src/common/java/be/elevenways/hohenheim/HohenheimEndpoints.java
  /home/skerit/projects/hohenext/hohenheim/src/server/java/be/elevenways/hohenheim/server/devtunnel/DevTunnelServerHandler.java
Tests added or changed: DevTunnelTest.aRotatedRegistrationTokenClosesTheLiveTunnel
  (4-step journey over the REAL client/proxy/tunnel: interval declared -> tunnel
  serves 200 -> operator rotates the token -> proxy answers 503 within 4 ticks).
Observed pre-fix failure: two runs, one per half of the defect.
  (a) endpoint declaration removed, handler hook present:
      [step 1: an in-band-authenticated socket must still be revalidated]
      org.opentest4j.AssertionFailedError: expected: 15000L but was: 0L
      (intervalFor answers 0 -> ZenitHttpServer creates NO revalidator)
  (b) endpoint declaration present, handler hook reverted to the default:
      [step 4: a rotated token must close the live tunnel]
      org.opentest4j.AssertionFailedError: Expecting value to be true but was false
      (60.5s: the tunnel served traffic for the whole window with a rotated token)
Verification command and result:
  zenit-dev test --browser --skip-deps --no-fail-fast --class DevTunnelTest
  -> 8 passed (rotation test 15.7s: real close, real proxy fallback)
Backend/browser coverage: SQLite (hohenheim is SQLite-only at runtime); real
  WebSocket + real Undertow proxy, no stubs.
Known limitations or decisions: cadence is 15s, shared with
  TERMINAL_REVALIDATION_INTERVAL_MS (was going to be 30s; lowered so the
  end-to-end close is provable inside the suite budget and so the two privileged
  WS ingresses share one number). The pre-registration window is still owned by
  the existing 10s auth timeout - revalidate() returns true there because an
  unregistered connection holds no authorization to lose.
Commit and push state: e9a0eb7, NOT pushed.
```

```text
Issue: E7 - Terminal CSP predicate broader than the real subpage
User-facing behavior changed: STRICT_ADMIN_TERMINAL ('wasm-unsafe-eval',
  connect-src data:) is now served only on a path that actually dispatches to
  SiteProcessesPage. 404s under a registered panel, registered resources without a
  processes subpage, and deeper paths ending in /page/processes get STRICT_ADMIN.
Architectural responsibility changed: the claim resolves the same registrations the
  request does -- PanelRegistry.getBySlug -> Panel.peerBySlug -> instanceof Resource
  -> RecordSubpageRegistry.INSTANCE.resolve(resource.subpages()) -- and requires the
  page under slug "processes" to BE a SiteProcessesPage. It stays STRUCTURAL (no
  record load, so a claim check never queries) and stays on
  ScopedCspMiddleware.installVariant / VARIANT_WEIGHT, so the overlap diagnostic
  still does not fire and STRICT_ADMIN was not broadened (G8 respected).
Files changed:
  /home/skerit/projects/hohenext/hohenheim/src/server/java/be/elevenways/hohenheim/server/cms/SiteTerminalCsp.java
Tests added or changed: TerminalCspClaimTest.terminalConcessionsRideOnlyTheProcessesSubpage
  extended from 4 to 8 numbered steps: (5) /admin/not-a-resource/1/page/processes
  (the 404 case the ledger names), (6) /admin/certificates/1/page/processes (a real
  resource with no processes subpage), (7) /admin/sites/1/anything/at/all/page/processes
  (depth), (8) /manage/sites/1/page/processes must KEEP the variant (the delegated
  panel really does carry the terminal - guards against over-narrowing).
Observed pre-fix failure:
  [step 5: a 404 under a registered panel must not get the terminal variant]
  java.lang.AssertionError: Expecting actual:
  "default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; connect-src 'self' data:; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; frame-ancestors 'none'; base-uri 'self'; object-src 'none'; form-action 'self'"
  not to be equal to:
  "default-src 'self'; script-src 'self' 'wasm-unsafe-eval'; connect-src 'self' data:; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; frame-ancestors 'none'; base-uri 'self'; object-src 'none'; form-action 'self'"
Verification command and result:
  zenit-dev test --browser --skip-deps --no-fail-fast --class TerminalCspClaimTest -> 1 passed
Backend/browser coverage: real HTTP against the real app, anonymous requests.
Known limitations or decisions: the predicate hard-codes the record-subpage route
  SHAPE ({panel}/{resource}/{id}/page/{slug}, 5 segments) because
  ScopedCspMiddleware's claim contract is Predicate<String> over the route path.
  Widening that to a conduit/resolved-endpoint-aware claim would be a framework
  change for one consumer; the registry walk already removes the actual defect.
Commit and push state: b772041, NOT pushed.
```

```text
Issue: E10 - Hohenheim starts HTTP before installing handlers and panels
User-facing behavior changed: a request arriving the instant the listener accepts is
  served by real handlers and registered panels. Previously GET /api/health returned
  404, every /admin/... URL 404'd, and a dev-tunnel or terminal upgrade hit
  IllegalStateException("WebSocket handler factory returned null").
Architectural responsibility changed: new discovered ZenitModule HohenheimHostWiring
  (the HohenheimWriteHooks precedent) owns the client script location,
  HohenheimHandlers.init(), both panel constructions and HohenheimSecurity.boot(),
  running at MODULES (weight 200) -- after MODELS (300), which is the ordering
  constraint the old ServerMain comment named, and before STARTHTTP (50) binds.
  ServerMain no longer wires anything request-facing after
  ServerZenitRuntime.main(args). The harness converged: HohenheimTestRuntime.ensureBooted()
  now installs auth before the boot stages (ZenitAuth.init is idempotent; auth
  BASELINES stay the caller's choice, being policy not wiring), and HohenheimTestBase
  no longer hand-installs handlers/panels/security after booting. DevTunnelTest's
  compensating `DEV_TUNNEL.setHandlerFactory(...)` line and its
  setClientScriptLocation line were REMOVED - nothing compensates any more.
Files changed:
  .../server/HohenheimHostWiring.java (new)
  .../server/ServerMain.java
  .../server/cms/HohenheimPanel.java (docblock: constructed by the module now)
  .../browserTest/.../HohenheimTestBase.java
  .../browserTest/.../HohenheimTestRuntime.java
  .../browserTest/.../DevTunnelTest.java
Tests added or changed: BootWiringWindowTest.everyRequestFacingWiringExistsWhenTheListenerAccepts
  (new, isolated bucket). It replays ServerMain's pre-boot sequence, pins a free
  port + AUTO_START_HTTP from a weight-400 boot stage (init() reloads settings
  synchronously before launching stages, so setting them beforehand is discarded),
  binds for real, and runs a probe boot stage at weight 49 -- immediately AFTER
  STARTHTTP (50) and before anything else. The probe issues a REAL HTTP GET
  /api/health and checks PanelRegistry plus DEV_TUNNEL.createHandler(null).
How the window was proved: not by re-booting the reordered code. The probe observes
  exactly what the first client of a fresh process observes, and pre-fix the real
  request was answered wrongly:
Observed pre-fix failure (HohenheimHostWiring deleted, ServerMain reverted):
  [step 3: GET /api/health at bind time must be handled]
  org.opentest4j.AssertionFailedError: expected: "200" but was: "404"
Verification command and result:
  zenit-dev test --browser --skip-deps --no-fail-fast --class BootWiringWindowTest -> 1 passed
  Full suite (justified: the boot order changed for EVERY browser test class, and
  the shared harness was rewritten):
  zenit-dev test --skip-deps --no-fail-fast -> 578 passed, 3 skipped, 124 of 124
  classes, 425s. hohenheim has no src/test (unit phase reports "no unit tests").
  zenit-dev build (full chain, caching on) -> all dependencies fresh, ok.
Backend/browser coverage: SQLite; real ZenitHttpServer bind + real java.net.http client.
Known limitations or decisions: the proxy listener, DNS server, spamservice manager,
  interrupted-deploy sweep and shutdown hook deliberately stay in ServerMain after
  main(). They are separate listeners/lifecycles with their own ordering
  requirements, not zenit HTTP endpoint wiring, and moving them would change
  shutdown ordering for no defect. HohenheimSecurity.boot() DID move (its
  SecurityEvents sink and ban cache feed request handling, and the harness already
  ran it pre-bind, so leaving it out would have kept a divergence). Several isolated
  test classes still call Zenit.getHawkeye().setClientScriptLocation("/cms.js") and
  HohenheimSecurity.boot() redundantly; both are idempotent and they are unrelated
  files, so they were left alone.
Commit and push state: dc68e5b, NOT pushed.
```

Cross-cutting notes:
- No zenit-side change was needed or made; zenit/zenit-auth/zenit-cms/plumage worktrees are untouched. The E4/E6/E8 route-matcher and revalidator changes at zenit 9fc2846 were built and relied on, never reverted.
- Counterfactuals used `--skip-deps` only; nothing reverted was published to Maven local. The worktree was byte-verified identical to the saved fix patch before committing.
- hohenheim HEAD is now b772041 (was 1a56057), worktree clean, nothing pushed.